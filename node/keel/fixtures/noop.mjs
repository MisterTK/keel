// The smallest possible program — used to measure the startup overhead
// `--import keelrun/hook` adds versus a plain `node` launch (DX invariant 8: "keel
// run adds <100ms to process startup at p50"). Mirrors
// python/keel/tests/fixtures/noop_app.py exactly: no work, no output.
