{
  "name": "keelrun-cli-linux-arm64-musl",
  "version": "0.4.0",
  "description": "Prebuilt `keel` CLI binary for Linux aarch64, statically linked (musl) — runs on glibc hosts too, so no libc restriction is declared here. Optional dependency of keelrun-cli — installed automatically on a matching platform, ignored otherwise. See scripts/cli-prebuild.sh.",
  "os": ["linux"],
  "cpu": ["arm64"],
  "main": "keel",
  "files": ["keel"],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MisterTK/keel.git",
    "directory": "node/keel-cli/npm/linux-arm64-musl"
  },
  "private": false,
  "engines": {
    "node": ">=18"
  }
}
