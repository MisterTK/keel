{
  "name": "keelrun-cli-linux-x64-musl",
  "version": "0.4.1",
  "description": "Prebuilt `keel` CLI binary for Linux x86_64, statically linked (musl) — runs on glibc hosts too, so no libc restriction is declared here. Optional dependency of keelrun-cli — installed automatically on a matching platform, ignored otherwise. See scripts/cli-prebuild.sh.",
  "os": ["linux"],
  "cpu": ["x64"],
  "main": "keel",
  "files": ["keel"],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MisterTK/keel.git",
    "directory": "node/keel-cli/npm/linux-x64-musl"
  },
  "private": false,
  "engines": {
    "node": ">=18"
  }
}
