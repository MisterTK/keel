# Keel — Developer Experience & Agent-Framework Specification
*Addendum to the Architecture Spec v0.1. This document is the product's soul: if the architecture spec defines what Keel is, this defines why anyone will love it. Every engineering decision that conflicts with this document loses.*

**North star:** a developer (human or AI) adds Keel to anything — a 20-line script or a multi-service system — in under 60 seconds, changes zero lines of code, and their software becomes production-grade. The feeling to engineer is the SQLite/ripgrep feeling: *"it just worked, and now I don't ship anything without it."*

---

## 1. The "just works" ladder

Each level is complete and valuable on its own. Nobody is ever forced up a level; everybody can see the next one from where they stand.

### Level 0 — Zero config, zero files, zero decisions

```
$ uvx keel run app.py            # python
$ npx keel run app.ts            # node
$ cargo keel run                 # rust
```

No `keel.toml` required. **Smart defaults ship in the binary:** every discovered outbound call gets conservative production defaults — 30s timeout, 3 retries on transient errors (connection, timeout, 429, 5xx) with jittered exponential backoff, per-host circuit breaker. LLM provider calls get the LLM default pack (§4.1). One line of output, then silence:

```
keel ▸ wrapped 14 call sites (httpx ×9, openai ×4, psycopg ×1) with production defaults — `keel init` to customize
```

**Hard rules for Level 0:** never change program semantics on the success path; never retry non-idempotent calls (POST without idempotency key) by default — mark them `observed, not retried` and say so in `keel status`; if Keel can't wrap something safely, it does nothing and notes it. Trust is the product; a Level 0 surprise is a P0 bug.

### Level 1 — One command generates your policy, from evidence

```
$ keel init
```

Auto-walks the project (§2), then writes a `keel.toml` that reads like a senior SRE reviewed your codebase:

```toml
# Generated by keel init from 2 static scans + 1 observed run (2026-07-11)
# Every entry below was found in YOUR code. Delete anything; defaults still apply.

[target."api.stripe.com"]            # seen in: billing.py:41, billing.py:88
retry       = { attempts = 5, schedule = "exp(200ms, x2, max 30s, jitter)" }
idempotency = { header = "Idempotency-Key" }   # Stripe supports this natively

[target."llm:openai"]                # seen in: agent.py:12 (gpt-4.1, ~220 calls/run observed)
retry    = { attempts = 6, on = ["429","5xx","timeout"] }
rate     = "60/min"                  # matches your observed peak of 41/min
cache    = { mode = "dev" }          # dev-loop cache; disabled when KEEL_ENV=prod
```

The generated file is the documentation. Comments cite file:line evidence and observed traffic. `keel init --diff` re-runs against a changed codebase and shows what it would add/remove.

### Level 2 — Durability, still config-only

```
$ keel flows suggest
  pipeline.ingest:main      12 effects, 3 idempotent-unsafe, est. replay-safe: YES
  jobs.nightly:run          31 effects, replay-safe: YES (2 time reads will be virtualized)
$ keel flows add pipeline.ingest:main
```

Crash it with ctrl-C mid-run; `keel run` resumes from step 7 of 12. The demo that sells the product.

### Level 3 — Fleet

One line (`journal = "postgres://…"`), nothing else changes. Covered in the architecture spec; from a DX standpoint the requirement is only: *the laptop experience never degrades to serve the fleet feature.*

---

## 2. Auto-walking: how Keel learns a codebase

Three evidence sources, merged, each labeled in output so trust is inspectable:

1. **Static scan** (`keel init` cold): Python via `ast`, TS via `oxc`/SWC parse, Rust via `syn` in a cargo subcommand. Finds imports of known effect libraries, call sites, URL/host literals, function defs matching effectful naming patterns. Fast, no execution, catches ~80%.
2. **Import-time discovery** (every `keel run`): what actually got loaded and wrapped — ground truth on coverage, catches dynamic imports the scan missed.
3. **Observed runs** (`.keel/discovery.db`, always on, cheap): real call frequencies, latencies, error rates, hosts hit. This is what makes `keel init` output *evidence-based* ("matches your observed peak of 41/min") instead of template boilerplate — the single biggest differentiator between Keel's onboarding and every YAML-template competitor.

`keel doctor` is the honesty report: what's wrapped, what's visible-but-unwrapped and why, what's invisible (raw sockets, unknown native libs), which adapters are version-pinned vs. best-effort, and — for agent frameworks — which framework was detected and which adapter pack got enabled. Everything `doctor` reports has a `--json` twin and a suggested action.

## 3. DX invariants (the contract with the developer)

Non-negotiable, tested in CI as behaviors:

1. **Zero code changes** in Python/TS, one optional attribute in Rust. No imports, no context objects, no base classes, ever.
2. **Uninstall = remove the package.** Code runs identically without Keel (minus resilience). No lock-in by construction.
3. **No daemon, no port, no login.** State is files in `.keel/` (gitignored by `keel init`).
4. **Silence on success, precision on failure.** No log spam. When something happens, the message is complete: `keel ▸ POST api.stripe.com failed 5/5 attempts over 47s (last: 503). Breaker OPEN for 15s. 2 requests queued. trace: keel trace 8f3a`.
5. **Never swallow errors.** After final failure the original exception/error propagates unchanged — same type, same stack, plus a `keel` attachment for those who look.
6. **Everything has `--json`** and stable exit codes. Humans get prose; machines get structure; same command.
7. **Works offline.** No telemetry, no phone-home, no update nag.
8. **Fast.** `keel run` adds <100ms to process startup at p50; wrapping overhead per call ≤10µs. Measured in CI, published in the README.

## 4. AI agent frameworks as first-class citizens

Agent code is the densest concentration of flaky effects in modern software — LLM calls, tool calls, MCP round-trips, sandboxed execution — and the frameworks have the weakest resilience stories (your own vendor doc: ADK has *no built-in retry/backoff; error handling is callback-based and left to the developer*). This is Keel's beachhead market, not an integration afterthought.

### 4.1 Semantic target classes

Beyond `host:` and function targets, Keel defines pseudo-targets that map to what agent developers actually think about:

- **`llm:<provider>`** — provider SDK calls (openai, anthropic, google-genai/Vertex, Bedrock, AI Gateway). Default pack: retry on 429/5xx/timeout with provider-aware backoff (respects `Retry-After`), token-bucket rate limiting, per-run cost/token budget caps (`budget = "$5/run"` → breaker trips), **model fallback chains** (`fallback = ["gemini-2.5-pro", "claude-sonnet-4.5"]`), and **dev-mode response caching** — identical prompt+params replays from cache during development. The dev cache alone (10× faster iteration, near-zero API spend while hacking on agent logic) is a killer feature that makes agent developers adopt Keel for selfish reasons before they ever need resilience.
- **`tool:<name>`** — agent tool invocations, wrapped at the framework's own boundary so retries happen *below* the LLM loop (a failed tool call is retried without burning tokens on a new LLM turn).
- **`mcp:<server>`** — MCP client transports (stdio + HTTP). Per-server timeout, retry, breaker; a hung MCP server degrades gracefully instead of freezing the agent.

### 4.2 Framework adapter packs (auto-detected, zero config)

`keel doctor` detects the framework from dependencies and enables the pack; each pack is maintained like a library adapter (version-pinned contract tests):

- **Google ADK (Python + TS):** a `keel-adk` plugin registered automatically at import time via ADK's plugin/callback API — tool and LLM calls become Keel targets; `on_tool_error` wiring becomes policy-driven instead of hand-written. This fills exactly the gap your vendor comparison flagged, with zero agent-code changes. For **google/agents-cli** users: `keel init` recognizes the scaffold layout and emits a policy file matching its structure, so ADK agents built the "official" way are covered by default.
- **LangGraph:** node execution wrapped as `tool:`/function targets (call-level resilience under the graph), plus — the strategic move — **a Keel-journal-backed checkpointer**, so graph state persistence and effect-level durability live in one file with one trace view instead of two disjoint systems.
- **Vercel eve:** eve is TypeScript, filesystem-first (agents are directories of tools/skills/subagents), runs conversations as durable workflows, and reaches the world through MCP servers and OpenAPI tools — which means Keel's Node loader + `mcp:` targets cover it naturally: wrap the tool modules eve discovers and the MCP transports it calls. Keel doesn't compete with eve's conversation-level durability; it hardens the *effects inside* each step, which eve doesn't do at call level. ([vercel/eve](https://github.com/vercel/eve), [InfoQ](https://www.infoq.com/news/2026/06/vercel-eve-agents/))
- **Vercel AI SDK:** first-class middleware (`wrapLanguageModel`) — the cleanest seam of any framework; the adapter is thin and safe.
- **Pydantic AI / OpenAI Agents SDK / CrewAI:** same pattern, prioritized by observed demand; the adapter-pack interface (§4.3) makes each a bounded, agent-team-sized task.

### 4.3 The adapter-pack contract

Every pack is a small, uniform module: `detect()` (from deps/imports), `seams()` (what it patches and why it's stable), `targets()` (semantic targets exposed), `defaults()` (its policy pack), plus contract tests against pinned framework versions. This uniformity is what makes "add framework X" a parallelizable one-team sprint task forever.

## 5. AI developers (Claude Code, Codex, Antigravity…) as first-class users

A growing share of Keel's users will be coding agents acting on a human's behalf. Design for them explicitly:

- **`keel mcp`** — the CLI doubles as an MCP server exposing: `get_status`, `get_doctor_report`, `propose_policy` (returns a keel.toml *diff*), `get_trace`, `list_flows`, `explain_error`. A coding agent can install, diagnose, and tune Keel end-to-end without parsing human prose.
- **Diffs as the lingua franca.** Every suggestion Keel makes — doctor fixes, init changes, flow proposals — is emitted as an applyable diff. Agents apply diffs reliably; they paraphrase prose unreliably.
- **`keel init --agents`** drops a concise section into AGENTS.md/CLAUDE.md ("this project uses Keel; policies live in keel.toml; run `keel doctor --json` before changing resilience behavior") so every future agent session inherits context.
- **Docs built for retrieval:** llms.txt + llms-full.txt, one canonical example per feature, stable headings, and a `keel explain <error-code>` command so an agent seeing `KEEL-E014` gets the exact remedy without a web search.
- **Determinism as courtesy:** identical inputs → byte-identical `--json` output (no timestamps unless asked, sorted keys) — agents diff outputs to detect change.

The compounding effect: when Claude Code scaffolds a new Python service in 2027 and its training/tooling has seen Keel everywhere, "add keel" becomes part of what "production-grade" means to the agents themselves. That — not marketing — is how a de facto standard forms. The prerequisite is that the agent-facing surface is *flawless*, which is why it's specified here and not left to emerge.

## 6. The experience details that make people evangelize

- **`keel status`** — one screen: wrapped coverage %, retries saved this week, breaker events, cache hit rate, flows resumable. The "what is Keel doing for me" answer, also the screenshot people post.
- **`keel tail`** — live view of attempts/backoffs/breaker transitions while your program runs. Watching your script *survive* a WiFi drop in real time is the conversion moment.
- **`keel record test`** — record a run's effects, replay them as an offline deterministic test fixture. Resilience tooling that also deletes your mock boilerplate.
- **Error messages hold both audiences:** first line for humans (what/why/what next), `--json` for machines, `keel explain` code for both.
- **Install surface:** `pip install keel` / `npm i -D keel` / `cargo add keel` plus `brew install keel` for the bare CLI. The Python/npm packages bundle prebuilt core binaries (maturin/napi-rs wheels) — no toolchain, no compile, ever.
- **The README demo is a 40-second asciinema:** flaky script fails → `uvx keel run` → survives → ctrl-C mid-flow → resumes. No architecture diagram above the fold.

## 7. What "nailing it" rules out

No web dashboard in v1 (the TUI is the dashboard; a hosted anything contradicts the story). No config DSL beyond TOML (no Python-in-config, no CEL — the moment policy needs a debugger, the product has failed). No "enterprise features" that leak into the individual experience (SSO, RBAC, audit live entirely behind the journal backend boundary). No plugin API in v1 (adapter packs are in-tree; a public plugin surface before the semantics stabilize creates a compatibility prison).

---

*Sources for framework facts: [vercel/eve](https://github.com/vercel/eve) · [Vercel: Introducing eve](https://vercel.com/blog/introducing-eve) · [InfoQ on eve](https://www.infoq.com/news/2026/06/vercel-eve-agents/) · [The New Stack on eve](https://thenewstack.io/vercel-launches-eve-an-open-source-framework-that-treats-agents-as-directories/) · ADK/agents-cli/LangGraph characteristics per the July 2026 vendor comparison in this project.*
